import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.co.modena.ciaomodena',
  appName: 'MODENA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
